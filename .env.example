# Optional GitHub personal access token (classic or fine-grained, read-only public scope).
# Blank  -> unauthenticated GitHub API: 60 requests/hour
# Set    -> authenticated GitHub API:   5,000 requests/hour
# The backend only reads public data, so a token with NO scopes is enough.
GITHUB_TOKEN=
